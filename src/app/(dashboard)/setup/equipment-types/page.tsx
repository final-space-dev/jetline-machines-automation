import { SetupShell } from "../setup-shell";

// Legacy deep link — renders the consolidated Setup page on the Equipment Types tab.
export default function EquipmentTypesPage() {
  return <SetupShell active="equipment-types" />;
}
